{
  "name": "bird-tagging-system",
  "version": "1.0.0",
  "description": "AWS-based bird species identification and tagging system",
  "main": "index.html",
  "scripts": {
    "start": "python -m http.server 8000",
    "serve": "npx http-server -p 8080 -c-1",
    "deploy": "aws s3 sync . s3://your-bucket-name --exclude '*.md' --exclude '.git/*'"
  },
  "keywords": [
    "aws",
    "machine-learning",
    "bird-identification",
    "cognito",
    "s3",
    "lambda"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
